function ColorMyPencils(color)
    --color = color or "noctis"
    --vim.cmd.colorscheme(color)

    -- bakcground color transparent
    --vim.api.nvim_set_hl(0, "Normal", { bh = "none"})
    -- for all floating windows too
    --vim.api.nvim_set_hl(0, "NormalFloat", { bh = "none"})
end

ColorMyPencils()
